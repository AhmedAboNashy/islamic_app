import 'package:flutter/material.dart';
class sebha extends StatelessWidget {
  const sebha({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(transformAlignment: Alignment.center,
      color: Colors.amber, child: Text('Sebha',style: TextStyle(color:Colors.white,fontSize: 40),),
    );
  }
}
